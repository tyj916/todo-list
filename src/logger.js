/*
  to log tasks and projects either to browser or to local storage
*/

export { logTask, logMessage };

function logTask(task) {
  console.log(task);
}

function logMessage(message) {
  console.log(message);
}